最新の LIMEs (`{{LIME_TAG}}`) を以下のバージョンのLINEに適用した自動リリースです。

### 提供バージョン
{{VERSION_LIST}}

### ファイルの選び方
- `LSPatch-Local.apk` : LSPatchをローカルで適用 別途 Manager アプリが必要
- `LSPatch-Embed.apk` : LSPatchを統合で適用 モジュール内蔵・単体で動作。ただし動作には別途LIMEsインストールは必須。
- `NPatch-Local.apk` : NPatchをローカルで適用 別途 Manager アプリが必要
- `NPatch-Embed.apk` : NPatchを統合で適用 モジュール内蔵・単体で動作。ただし動作には別途LIMEsインストールは必須。

*Source Module: [areteruhiro/LIMEs](https://github.com/areteruhiro/LIMEs/releases/tag/{{LIME_TAG}})*
